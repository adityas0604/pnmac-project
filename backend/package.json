{
  "name": "pnmac-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "serverless deploy",
    "dev": "serverless offline",
    "seed": "node shared/seeddb.js",
    "build:cron": "rm -rf dist && mkdir -p dist/cron && esbuild cron/handler.js --bundle --platform=node --target=node20 --format=esm --outfile=dist/cron/handler.js && cd dist && zip -r cron.zip cron"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/adityas0604/pnmac-project.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "bugs": {
    "url": "https://github.com/adityas0604/pnmac-project/issues"
  },
  "homepage": "https://github.com/adityas0604/pnmac-project#readme",
  "devDependencies": {
    "dotenv": "^17.2.4",
    "serverless": "^3.40.0",
    "serverless-offline": "^14.4.0"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.985.0",
    "@aws-sdk/client-secrets-manager": "^3.985.0",
    "@aws-sdk/lib-dynamodb": "^3.985.0",
    "@massive.com/client-js": "^10.3.0"
  }
}
